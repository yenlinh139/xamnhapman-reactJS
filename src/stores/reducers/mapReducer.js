// mapReducer.js
const initialState = {
    farmBoundaries: [],
};

const farmReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default farmReducer;
